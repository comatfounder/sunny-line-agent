[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn sunny_line_server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
